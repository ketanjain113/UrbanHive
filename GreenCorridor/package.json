{
  "name": "emergency-traffic-coordination-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "backend:dev": "npm --prefix Backend run dev",
    "backend:start": "npm --prefix Backend run start",
    "dev": "npm --prefix Frontend run dev",
    "build": "npm --prefix Frontend ci && npm --prefix Backend ci && npm --prefix Frontend run build",
    "build:frontend": "npm --prefix Frontend run build",
    "preview": "npm --prefix Frontend run preview",
    "start": "npm --prefix Backend ci && npm --prefix Backend run start",
    "railway:build": "npm run build",
    "render:build": "npm run build"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
